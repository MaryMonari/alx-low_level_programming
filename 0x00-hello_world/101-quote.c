#include <stdio.h>

/**
 * main - main function
 * prints exactly and that piece of art is useful followed by a new line
 * Return: end program
 */
int main(void)
{
	char n[] = "and that piece of art is useful\" - Dora Korpar, 2015-10-19";

	fprintf(stderr, "%s\n", n);

	return (1);
}
